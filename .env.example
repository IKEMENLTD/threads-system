# 環境変数の例（.envファイルにコピーして使用）

# MongoDB接続文字列（MongoDB Atlas）
MONGODB_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/threads_system?retryWrites=true&w=majority

# JWT秘密鍵
JWT_SECRET=your-secret-key-change-this

# Node環境
NODE_ENV=production

# ポート
PORT=3000